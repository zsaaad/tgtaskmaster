// Copy this file to `firebase-config.js` and fill in real values from your
// Firebase console (Project Settings → Web app → SDK setup and configuration).
// `firebase-config.js` is in .gitignore — it never gets committed.
//
// If `firebase-config.js` does not exist (or the placeholders below are still
// in place), the app falls back to single-user localStorage mode and prints a
// console notice. Real-time sync only kicks in when this file is present and
// values are real.

window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
